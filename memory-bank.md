# Astra Memory Bank

Astra Discord Suite is a multi-server Discord automation, moderation, ticketing, AI, and subscription-plan dashboard built with Node.js, TypeScript, Express, Discord.js, React, Vite, and SQLite.

The canonical, source-verified project record is [`PROJECT_CONTEXT.md`](./PROJECT_CONTEXT.md). Read it before making changes and update it in the same change whenever architecture, routes, plans, storage, setup, or major features change. Do not duplicate detailed project state here; this pointer is intentionally short so the two files cannot drift.

Never store `.env` values, Discord credentials, session secrets, provider keys, or runtime database contents in either context document.
